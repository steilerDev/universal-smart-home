# Copy this file to secrets.yaml and fill in real values.
# secrets.yaml is gitignored and must never be committed.
#
# Keys used across packages:
#   esphome_ota_password       — packages/base/room-sensor.yaml
#   home_assistant_encryption  — packages/base/room-sensor.yaml

esphome_ota_password: "CHANGE_ME_STRONG_RANDOM"
home_assistant_encryption: "CHANGE_ME_32_BYTE_BASE64="
